<h1>Upload a new image:</h1>

<%= form_for(@image, url: admin_images_path) do |f| %>
  <%= f.label :url %>
  <%= f.text_field :url %>
  <div class="form_group">
    <%= f.label :avatar %>
    <%= f.file_field :avatar %>
  </div>
  <%= f.submit "Create"%>
<% end %>
